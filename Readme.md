Make sure the python mysql module is installed:

    pip install mysql-connector-python
    
Make sure the python sty module is installed, this gives us nice colors:

    pip install sty
   

